def send_notification(order_id):
    print(f'УВЕДОМЛЕНИЕ: Заказ №{order_id} обработан')


def send_bad_notification(order_id):
    print(f'УВЕДОМЛЕНИЕ: Произошла ошибка при обработке Заказа №{order_id}')
